using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pulmon : Organ_Be
{
    // Start is called before the first frame update
    void Start()
    {
        canbeused = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
